system:
  camera_index: 0
  line_margin: 20
  scan_cooldown_sec: 0.5

paths:
  db_path: 'data/inventory.json'
  yolo_model: 'models/new_checkout.pt'

ui:
  headless: false
  dashboard_width: 200
  flash_frames: 15
